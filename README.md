# Query Inspector

```
Missing expected flag --input!

Usage: query-inspect --input <path> [--dialect <value>]

No run, just inpect

Options and flags:
    --help
        Display this help text.
    --input <path>
        Input file path.
    --dialect <value>
        Query dialect. Supported: 'hive'(default), 'oracle'
```
